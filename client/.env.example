# API Base URL
VITE_API_BASE_URL=https://lab-server.lan/wss/

# WebSocket URL для подписки на статусы устройств
# 
# Локальная разработка (без nginx):
VITE_STATUS_WS_URL=ws://localhost:8081

# Продакшн через nginx (путь):
# VITE_STATUS_WS_URL=wss://ddweed.org/ws/status
#
# Продакшн через nginx (прямой порт):
# VITE_STATUS_WS_URL=wss://ddweed.org:8081
